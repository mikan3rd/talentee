query getTiktokRankingPage($pagination: PaginationInput!) {
  getTiktokRankingPage(pagination: $pagination) {
    uniqueId
    nickname
    signature
    avatarThumb
    followerCount
    heartCount
    videoCount
    account {
    uuid
    }
  }
}